<p style="color: green"><%= notice %></p>

<%= render @arriendo %>

<div class="d-flex gap-2">
  <%= link_to "Volver a Arriendos", arriendos_path, class:"btn btn-secondary" %>
  <%= link_to "Editar Arriendo", edit_arriendo_path(@arriendo),class:"btn btn-info" %>
  <%= button_to "Eliminar este arriendo", @arriendo, method: :delete, class:"btn btn-danger" %>
</div>
